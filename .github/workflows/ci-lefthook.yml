# This File is automatically synchronized from https://github.com/Glatzel/template

name: Lefthook

on:
  workflow_dispatch:
  workflow_call:
permissions:
  contents: write
  pull-requests: write
jobs:
  lefthook:
    runs-on: ubuntu-latest
    steps:
      - uses: Glatzel/template/.github/actions/common/lefthook@v2026.8.25
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
