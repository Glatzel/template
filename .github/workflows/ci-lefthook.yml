# This File is automatically synchronized from https://github.com/Glatzel/template

name: Lefthook

on:
  workflow_dispatch:
  workflow_call:
  issue_comment:
    types: [created]
permissions:
  contents: write
  pull-requests: write

jobs:
  lefthook:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event_name == 'pull_request' || (github.event_name == 'issue_comment' && github.event.issue.pull_request != null && contains(github.event.comment.body, 'lefthook')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: Glatzel/template/.github/actions/common/lefthook@ea63917412b3b2fa561a8575834475f2be73353b # v2026.8.29
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
