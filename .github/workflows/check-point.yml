# This File is automatically synchronized from https://github.com/Glatzel/template

name: check-point

on:
  workflow_dispatch:
jobs:
  check-point:
    if: ( cancelled() || failure() ) && github.event_name == 'pull_request'
    needs: [rattler-build, dispatch]
    runs-on: ubuntu-slim
    steps:
      - run: exit 1