name: pytest

on:
    workflow_call:
      inputs:
        name:
          required: true
          type: string
        machine:
          required: true
          type: string
        submodule:
          type: boolean
          default: false
        test_path:
          default: ./tests
          type: string
        python_path:
          type: string
          default: ""
        pixi_python_env:
            type: string
            default: default
        test_args:
          type: string
          default: ""
        cov:
          type: boolean
          default: false
        cov_args:
          type: string
          default : ""
        cov_thr:
          type: number
          default: 70
        
permissions:
  contents: read
  actions: read
  checks: write
jobs:
  pytest:
    strategy:
      matrix:
        machines: ${{inputs.machine}}
        pixi_python_envs: ${{inputs.pixi_python_env}}
    runs-on: ${{matrix.machines}}
    steps:
      - uses: actions/checkout@v4

      - name: Update Submodule
        if: ${{inputs.submodule}}
        run: git submodule update --init --recursive

      - name: Set up pixi
        uses: prefix-dev/setup-pixi@v0.8.1
        with:
          run-install: false

      - name: pytest
        if: runner.os == 'Windows' && success()
        run: |
          $Env:PYTHONPATH = "${{inputs.python_path}}"
          pixi run -e ${{matrix.pixi_python_envs}} pytest "${{inputs.test_path}}" --junit-xml results/pytest.xml ${{inputs.cov_args}} ${{inputs.test_args}}
      - name: Test Report Title
        run: |
          "## Test Report"| Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append

      - name: Test Report
        uses: dorny/test-reporter@main
        if: success() || failure()    
        with:
          name: Pytest Report (${{inputs.name}})
          path: results/pytest.xml
          reporter: java-junit
          use-actions-summary: "true"

      - name: Coverage Summary
        if: ${{inputs.cov}} && (success() || failure())
        run: |
          "## Coverage Summary"| Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
          pixi run -e ${{matrix.pixi_python_envs}} coverage report --skip-covered --format=markdown | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
          pixi run -e ${{matrix.pixi_python_envs}} coverage report --skip-covered --fail-under=${{inputs.cov_thr}}
