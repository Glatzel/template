name: ci

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  lefthook:
    permissions:
      contents: write
      issues: write
    uses: ./.github/workflows/ci-lefthook.yml
